import { Router } from 'express';

/**
 * Dashboard API (REQ-006–011) — placeholder registered by PKG-1 (tasks.md §3.3).
 * Implemented by PKG-4. Mounted behind requireAuth at /api/dashboard.
 */
export const dashboardRouter = Router();
